apiVersion: apps/v1
kind: Deployment
metadata:
  name: signalboard-backend
spec:
  template:
    spec:
      containers:
        - name: backend
          env:
            - name: DB_POOL_MAX
              value: "10"
